import type { BillingPayment } from "@/lib/types";
import { formatDate, formatMoney } from "./format";

export function BillingHistory({ payments }: { payments: BillingPayment[] }) {
  return <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="border-b border-slate-200 p-5"><h2 className="font-black">Payment history</h2><p className="mt-1 text-xs text-slate-500">Your latest Razorpay payment attempts.</p></div><div className="overflow-x-auto"><table className="w-full min-w-[640px] text-left text-xs"><thead className="bg-slate-50 text-slate-500"><tr><th className="px-5 py-3">Date</th><th className="px-5 py-3">Plan</th><th className="px-5 py-3">Payment ID</th><th className="px-5 py-3">Amount</th><th className="px-5 py-3">Status</th></tr></thead><tbody>{payments.map((payment) => <tr key={payment.id} className="border-t border-slate-100"><td className="px-5 py-4">{formatDate(payment.created_at)}</td><td className="px-5 py-4 font-bold">{payment.plan.name}</td><td className="px-5 py-4 font-mono text-[10px] text-slate-500">{payment.provider_payment_id || payment.provider_order_id || "Pending"}</td><td className="px-5 py-4 font-bold">{formatMoney(payment.amount_minor, payment.currency)}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 font-bold ${payment.status === "captured" ? "bg-emerald-50 text-emerald-700" : payment.status === "failed" ? "bg-rose-50 text-rose-700" : "bg-amber-50 text-amber-700"}`}>{payment.status.replace("_", " ")}</span></td></tr>)}{!payments.length && <tr><td colSpan={5} className="px-5 py-12 text-center text-slate-400">No payments yet.</td></tr>}</tbody></table></div></section>;
}
